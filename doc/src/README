INET Framework Documentation
============================

Building the documentation website
----------------------------------

The INET documentation can be built with the sphinx tool.

First install the necessary packages:

sudo apt-get install dvipng texlive-binaries
sudo pip install -U Sphinx
sudo pip install sphinx_materialdesign_theme
sudo pip install recommonmark
sudo pip install sphinxcontrib-doxylink
sudo pip install sphinx-autobuild
sudo pip install sphinxcontrib-inlinesyntaxhighlight

Use 'make html' or 'make pdf' to build the documentation.
Use 'make deploy' to build the html version and deploy it on the web.