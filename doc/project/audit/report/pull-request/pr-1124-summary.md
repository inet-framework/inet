# Change summary — INET pull request #1124, "ieee80211: make rate control adapt per receiver, add per-receiver configured rates"

> **Kind:** report · **Status:** generated, do not edit · **Seal:** none · **Owns:** — · **Stands on:** [rule/pull-request.md](../../../rule/pull-request.md), [design/protocol-anatomy.md](../../../design/protocol-anatomy.md)

Base `e38566236b` (merge base with master) to head `14725f1d61` — 6 commits, 24 files changed, 602 insertions(+), 140 deletions(-).

Generated by `opp_summarize_changes --pr 1124 --usage` (opp_repl). Re-generate it rather than editing it; the process is `opp_repl/plan/pending/change-summary.md`. The companion rule audit is [pr-1124.md](pr-1124.md): that one judges the *commits* against `PR-*`, this one states what the change does to the *interface*.

Extracted 2026-09-11 from the sources in 1.4 seconds; no build was needed. NED and message facts come from `opp_nedtool` and `opp_msgtool`, so they are those parsers' own view of the tree. C++ facts come from the fast header scan and are **indicative**: it joins multi-line declarations and excludes generated `*_m.h`, but it does not expand macros or resolve templates, so a declaration built by a macro is invisible to it.

## In one line

**15** added; **11** with a changed signature.

## Breaking — check these first

### Removed

_nothing removed_

### Changed

_nothing changed in place_

### Signature changed

**3 C++ public functions**

| What | Change |
|---|---|
| `AarfRateControl::getRate` | `()` → `(const MacAddress &)` |
| `IRateControl::getRate` | `()` → `(const MacAddress &)` |
| `OnoeRateControl::getRate` | `()` → `(const MacAddress &)` |

**8 C++ extension points**

| What | Change |
|---|---|
| `AarfRateControl::increaseRateIfTimerIsExpired` | `()` → `(State &)` |
| `AarfRateControl::multiplyIncreaseThreshold` | `(double)` → `(State &, double)` |
| `AarfRateControl::resetIncreaseThreshdold` | `()` → `(State &)` |
| `AarfRateControl::resetTimer` | `()` → `(State &)` |
| `OnoeRateControl::computeMode` | `()` → `(State &)` |
| `OnoeRateControl::computeModeIfTimerIsExpired` | `()` → `(State &)` |
| `OnoeRateControl::resetStatisticalVariables` | `()` → `(State &)` |
| `RateControlBase::emitDatarateChangedSignal` | `()` → `(const MacAddress &, const physicallayer::IIeee80211Mode *)` |

### Renamed

_nothing renamed_

## Added

**2 NED parameters**

- `inet.linklayer.ieee80211.mac.rateselection.QosRateSelection.dataFrameBitratePerReceiver`
- `inet.linklayer.ieee80211.mac.rateselection.RateSelection.dataFrameBitratePerReceiver`

**2 NED statistics**

- `inet.linklayer.ieee80211.mac.ratecontrol.AarfRateControl.dataratePerStation`
- `inet.linklayer.ieee80211.mac.ratecontrol.OnoeRateControl.dataratePerStation`

**2 C++ public functions**

- `IRateSelection::emitDatarateSelected(cComponent *, const Ptr<const Ieee80211MacHeader> &, const physicallayer::IIeee80211Mode *)` — 2 src, 0 tests
- `L3AddressResolver::getHostNameWithMacAddress(const MacAddress &)` — 2 src, 0 tests, **overridden nowhere**

**9 C++ extension points**

- `AarfRateControl::getState(const MacAddress &)` — 37 overrides
- `AarfRateControl::resetRateControl()`
- `OnoeRateControl::getState(const MacAddress &)` — 37 overrides
- `OnoeRateControl::resetRateControl()`
- `QosRateSelection::ensurePerReceiverModesResolved()` — 1 override
- `RateControlBase::getInitialMode()` — **overridden nowhere**
- `RateControlBase::getReceiverAddress(Packet *)` — **overridden nowhere**
- `RateControlBase::resetRateControl()` — 2 overrides
- `RateSelection::ensurePerReceiverModesResolved()` — 1 override

## Questions for the review

Counted by name over `src/` and `tests/`, so *uncalled* is exact and everything else is a hint. An override is not listed: its visibility is fixed by its base, and fulfilling a hook is not a new promise.

**3 new virtual(s) that nothing overrides** — what would an override do, and does a comment say so? ([AR-EXT-VIRTUAL-IS-A-PROMISE](../../../rule/architecture.md#ar-ext-virtual-is-a-promise))

- `L3AddressResolver::getHostNameWithMacAddress(const MacAddress &)`
- `RateControlBase::getInitialMode()`
- `RateControlBase::getReceiverAddress(Packet *)`

## Not changed

C++ classes · project features · source folders · message enum values · message fields · message types · NED gates · NED properties · NED signals · NED types
