# Change summary — INET pull request #1175, "ieee80211: AP HT discovery state and runtime channel changes"

> **Kind:** report · **Status:** generated, do not edit · **Seal:** none · **Owns:** — · **Stands on:** [rule/pull-request.md](../../../rule/pull-request.md), [design/protocol-anatomy.md](../../../design/protocol-anatomy.md)

Base `53acf0a633` (merge base with master) to head `843995a4d0` — 3 commits, 10 files changed, 1258 insertions(+), 11 deletions(-).

Generated by `opp_summarize_changes --pr 1175 --usage` (opp_repl). Re-generate it rather than editing it; the process is `opp_repl/plan/pending/change-summary.md`. The companion rule audit is [pr-1175.md](pr-1175.md): that one judges the *commits* against `PR-*`, this one states what the change does to the *interface*.

Extracted 2026-09-10 from the sources in 1.4 seconds; no build was needed. NED and message facts come from `opp_nedtool` and `opp_msgtool`, so they are those parsers' own view of the tree. C++ facts come from the fast header scan and are **indicative**: it joins multi-line declarations and excludes generated `*_m.h`, but it does not expand macros or resolve templates, so a declaration built by a macro is invisible to it.

## In one line

**2** added.

## Breaking — check these first

### Removed

_nothing removed_

### Changed

_nothing changed in place_

### Signature changed

_no signature changed_

### Renamed

_nothing renamed_

## Added

**2 C++ public functions**

- `Ieee80211Mib::getConfiguredSecondaryChannelOffset()` — **tests only**
- `Ieee80211Mib::setPrimaryChannel(int, const physicallayer::IIeee80211Band *)` — 1 src, 4 tests

## Questions for the review

Counted by name over `src/` and `tests/`, so *uncalled* is exact and everything else is a hint. An override is not listed: its visibility is fixed by its base, and fulfilling a hook is not a new promise.

**1 new public function(s) called only from tests** — is the test testing behavior, or reaching in? ([AR-EXT-MINIMAL-SURFACE](../../../rule/architecture.md#ar-ext-minimal-surface))

- `Ieee80211Mib::getConfiguredSecondaryChannelOffset()`

## Not changed

C++ classes · C++ extension points · project features · source folders · message enum values · message fields · message types · NED gates · NED parameters · NED properties · NED signals · NED statistics · NED types
