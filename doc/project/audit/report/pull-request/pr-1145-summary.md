# Change summary — INET pull request #1145, "ieee80211: HT guard interval, dynamic mode switching and transactional mode sets"

> **Kind:** report · **Status:** generated, do not edit · **Seal:** none · **Owns:** — · **Stands on:** [rule/pull-request.md](../../../rule/pull-request.md), [design/protocol-anatomy.md](../../../design/protocol-anatomy.md)

Base `cae555ebc8` (merge base with master) to head `1a30494752` — 13 commits, 56 files changed, 3260 insertions(+), 309 deletions(-).

Generated by `opp_summarize_changes --pr 1145 --usage` (opp_repl). Re-generate it rather than editing it; the process is `opp_repl/plan/pending/change-summary.md`. The companion rule audit is [pr-1145.md](pr-1145.md): that one judges the *commits* against `PR-*`, this one states what the change does to the *interface*. **Read the four removed `getMode` and `findMode` entries with the audit's third note: they are four signatures extended by one defaulted parameter, not four removals.**

Extracted 2026-09-10 from the sources in 1.4 seconds; no build was needed. NED and message facts come from `opp_nedtool` and `opp_msgtool`, so they are those parsers' own view of the tree. C++ facts come from the fast header scan and are **indicative**: it joins multi-line declarations and excludes generated `*_m.h`, but it does not expand macros or resolve templates, so a declaration built by a macro is invisible to it.

## In one line

**50** added; **4** removed; **4** changed.

## Breaking — check these first

### Removed

**4 C++ public functions**

- `Ieee80211ModeSet::findMode(bps, Hz, int)`
- `Ieee80211ModeSet::findMode(bps, bps, Hz, int)`
- `Ieee80211ModeSet::getMode(bps, Hz, int)`
- `Ieee80211ModeSet::getMode(bps, bps, Hz, int)`

### Changed

**4 C++ classes**

| What | Attribute | Change |
|---|---|---|
| `Ieee80211Mac` | bases | **+** `public physicallayer::IIeee80211ModeSetListener` |
| `Ieee80211MgmtBase` | bases | **+** `public physicallayer::IIeee80211ModeSetListener` |
| `ModeSetListener` | bases | **+** `public physicallayer::IIeee80211ModeSetListener` |
| `RateSelection` | bases | **+** `public physicallayer::IIeee80211ModeSetListener` |

### Signature changed

_no signature changed_

### Renamed

_nothing renamed_

## Added

**2 NED parameters**

- `inet.linklayer.ieee80211.mac.rateselection.QosRateSelection.dataFrameGuardInterval`
- `inet.linklayer.ieee80211.mac.rateselection.RateSelection.dataFrameGuardInterval`

**1 C++ class**

- `IIeee80211ModeSetListener`

<details><summary><b>42 C++ public functions</b></summary>

- `Dcaf::applyModeSet(const physicallayer::Ieee80211ModeSet *)`
- `Edcaf::applyModeSet(const physicallayer::Ieee80211ModeSet *)`
- `IIeee80211DataMode::getGuardInterval()` — 7 src, 1 tests, 4 overrides
- `IIeee80211Mode::getDataDuration(b)` — 19 src, 1 tests, 8 overrides
- `IIeee80211Mode::getHeaderDuration()` — 17 src, 1 tests, 8 overrides
- `IIeee80211Mode::getPreambleDuration()` — 17 src, 1 tests, 9 overrides
- `IIeee80211ModeSetListener::applyModeSet(const Ieee80211ModeSet *)` — 17 src, 0 tests, 8 overrides
- `IIeee80211ModeSetListener::getModeSet()` — 12 src, 10 tests, 4 overrides
- `IIeee80211ModeSetListener::~IIeee80211ModeSetListener()`
- `Ieee80211HtDataMode::getGuardInterval()`
- `Ieee80211HtMode::getDataDuration(b)`
- `Ieee80211HtMode::getHeaderDuration()`
- `Ieee80211HtMode::getPreambleDuration()`
- `Ieee80211HtPreambleMode::getDurationBeforeHeader()` — 4 src, 0 tests, 2 overrides
- `Ieee80211Mac::applyModeSet(const physicallayer::Ieee80211ModeSet *)`
- `Ieee80211Mac::getModeSet()`
- `Ieee80211MgmtBase::applyModeSet(const physicallayer::Ieee80211ModeSet *)`
- `Ieee80211MgmtBase::getModeSet()`
- `Ieee80211Mib::setPrimaryChannel(int, const physicallayer::IIeee80211Band *)` — 1 src, 4 tests
- `Ieee80211ModeSet::findCompatibleMode(const IIeee80211Mode *)` — 1 src, 1 tests
- `Ieee80211ModeSet::findMode(bps, Hz, int, simtime_t)` — **tests only**
- `Ieee80211ModeSet::findMode(bps, bps, Hz, int, simtime_t)` — **tests only**
- `Ieee80211ModeSet::getMandatoryModeAtOrBelow(const IIeee80211Mode *)` — 2 src, 1 tests
- `Ieee80211ModeSet::getMode(bps, Hz, int, simtime_t)` — 30 src, 11 tests
- `Ieee80211ModeSet::getMode(bps, bps, Hz, int, simtime_t)` — 30 src, 11 tests
- `Ieee80211OfdmDataMode::getGuardInterval()`
- `Ieee80211Radio::setModeSetAndMode(const Ieee80211ModeSet *, const IIeee80211Mode *)` — 2 src, 2 tests, 1 override
- `Ieee80211Receiver::getModeSet()` — 11 src, 10 tests
- `Ieee80211Transmitter::getMode()` — 30 src, 11 tests
- `Ieee80211Transmitter::getModeSet()` — 11 src, 10 tests
- `Ieee80211Transmitter::setModeSetAndMode(const Ieee80211ModeSet *, const IIeee80211Mode *)` — 2 src, 2 tests, 1 override
- `Ieee80211VhtDataMode::getGuardInterval()`
- `Ieee80211VhtMode::getDataDuration(b)`
- `Ieee80211VhtMode::getHeaderDuration()`
- `Ieee80211VhtMode::getPreambleDuration()`
- `Ieee80211VhtPreambleMode::getDurationBeforeHeader()` — 4 src, 0 tests, 2 overrides
- `ModeSetListener::applyModeSet(const physicallayer::Ieee80211ModeSet *)`
- `ModeSetListener::getModeSet()`
- `QosRateSelection::applyModeSet(const physicallayer::Ieee80211ModeSet *)`
- `RateControlBase::applyModeSet(const physicallayer::Ieee80211ModeSet *)`
- `RateSelection::applyModeSet(const physicallayer::Ieee80211ModeSet *)`
- `RateSelection::getModeSet()`

</details>

**3 C++ extension points**

- `Ieee80211Mac::receiveSignal(cComponent *, simsignal_t, cObject *, cObject *)`
- `QosRateSelection::updateModes()` — 1 override
- `RateSelection::updateModes()` — 1 override

**2 source folders**

- `inet/physicallayer/wireless/ieee80211/contract`
- `inet/physicallayer/wireless/ieee80211/contract/packetlevel`

## Questions for the review

Counted by name over `src/` and `tests/`, so *uncalled* is exact and everything else is a hint. An override is not listed: its visibility is fixed by its base, and fulfilling a hook is not a new promise.

**2 new public function(s) called only from tests** — is the test testing behavior, or reaching in? ([AR-EXT-MINIMAL-SURFACE](../../../rule/architecture.md#ar-ext-minimal-surface))

- `Ieee80211ModeSet::findMode(bps, Hz, int, simtime_t)`
- `Ieee80211ModeSet::findMode(bps, bps, Hz, int, simtime_t)`

## Not changed

project features · message enum values · message fields · message types · NED gates · NED properties · NED signals · NED statistics · NED types
