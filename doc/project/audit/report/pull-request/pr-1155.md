# Pull request audit report — PR #1155 "tcp: modernize the TCP model"

> **Kind:** report · **Status:** snapshot 2026-09-11 · **Seal:** none · **Owns:** — · **Stands on:** [rule/pull-request.md](../../../rule/pull-request.md), [rule/release.md](../../../rule/release.md), [rule/sealing.md](../../../rule/sealing.md), [rule/architecture.md](../../../rule/architecture.md)

- **Scope:** <https://github.com/inet-framework/inet/pull/1155>, head `33e8b0d073`, merge base
  `434658d729`. **61 commits, 183 files, +15840 / −3685.**
- **Date:** 2026-09-11. **Re-audit.** The first audit, on 2026-09-01, covered **the same head**:
  `33e8b0d073` then and now, the same merge base, the same 61 commits, the same diffstat. **The
  branch has not changed.**
- **Author:** Rudolf Hornig. **State:** open.
- **Why re-audit an unchanged branch:** two things around it moved. **Master advanced 99 commits**,
  and the branch no longer merges (F-6). And **four rules were added or inverted** after the first
  audit, so the change is now measured against checks that did not exist when it was written:
  `AR-ORG-CONTRACT-PURITY` (2026-09-07), `AR-EXT-MINIMAL-SURFACE` and
  `AR-EXT-VIRTUAL-IS-A-PROMISE` (2026-09-09), and the inversion of `PR-SPLIT-BASELINE`
  (2026-09-01). This is the case [SR-RULE-CHANGE-STALES](../../../rule/sealing.md#sr-rule-change-stales)
  describes, applied to a report rather than a seal.
- **Companion:** [pr-1155-summary.md](pr-1155-summary.md) — **547 added, 49 removed, 22 changed, 2
  with a changed signature, 18 renamed, 22 renamed with their class, 5 moved to another class, 4
  split across classes.** The last three groups are new on 2026-09-11: the summary previously read
  31 of them as removals beside unrelated additions, which overstated what this change removes.
- **Result:** **FAIL — 3 blocking findings, 4 more, 3 notes.** Three of the seven are new. Two come
  from rules written since; the third (F-7) is a fault both earlier passes missed, and this report
  had it as a PASS.

## What changed since 2026-09-01

| | First audit | Now |
|---|---|---|
| the branch | `33e8b0d073` | **identical** |
| merges onto master | yes | **no — 3 conflicting paths (F-6)** |
| `AR-ORG-CONTRACT-PURITY` | rule did not exist | **`ITcpRecovery` holds 5 default bodies (F-5)** |
| `AR-EXT-MINIMAL-SURFACE`, `AR-EXT-VIRTUAL-IS-A-PROMISE` | rules did not exist | **28 uncalled, 95 unoverridden — see the notes** |
| `PR-SPLIT-BASELINE` | the branch followed the rule of the day | the rule inverted; F-3 is now plain |
| `PR-MSG-BODY` | rule did not exist; both passes read only the bodies that were there | **written 2026-09-11; 17 of 61 commits have none (F-7)** |
| finding severities | every fault numbered | the ten subject lengths are a note |
| `common/packet/` seal | sealed over two unsanctioned clusters | **unchanged; still `Open (decide)`** |

## How the audit was made

```bash
git fetch origin refs/pull/1155/head:refs/pr/1155 --force        # head unchanged: 33e8b0d073
git merge-tree --write-tree origin/master refs/pr/1155           # 3 conflicts
doc/project/enforcement/check-commits.sh $MB..refs/pr/1155
opp_summarize_changes --repo . --pr 1155 --usage -o pr-1155-summary.md
doc/project/enforcement/check-interfaces.sh --verbose             # head 17, master 15
doc/project/enforcement/check-architecture.sh                     # head 25, master 25
```

Every `--usage` number below was spot-checked by hand against `git grep`, because that pass returned
confident wrong answers twice before ([#1175](pr-1175.md) §*A correction to this report's own
instrument*).

## Verdicts

| Rule | Verdict | Evidence |
|---|---|---|
| PR-SPLIT-ONE-CHANGE | PASS | 61 subjects, 61 decisions |
| PR-SPLIT-WHITESPACE | PASS | gate clean |
| PR-SPLIT-MECHANICAL | PASS | the rename sweep is its own commit |
| PR-SPLIT-MOVE | **FLAG (F-4)** | `ed2c8df034` renames 3 files and edits 25 |
| PR-SPLIT-PREPARE | PASS | the split architecture lands before the algorithms that use it |
| PR-SPLIT-UPSTREAM | PASS | the packet, PPP and contract changes come first, in their own commits |
| PR-SPLIT-BASELINE | **FLAG (F-3)** | two baseline-only commits |
| PR-SPLIT-DRIVEBY | PASS | no unrelated hunk found |
| PR-SERIES-BUILDS | not verified | 61 commits; needs a per-commit CI build |
| PR-SERIES-ORDER | PASS | no fixup, no "address review" |
| PR-SERIES-LINEAR | PASS | 0 merge commits |
| PR-MSG-SUBJECT | PASS, with a note | 10 of 61 between 73 and 80, inside the band the gate allows |
| PR-MSG-BODY | **FLAG (F-7)** | **17 of 61 commits have no body**, 12 of them above the gate's line |
| PR-MSG-WHY | **FLAG (F-7)** | where a body exists it is good; 17 times there is none to judge |
| PR-MSG-FACTS | PASS | no attribution trailer |
| PR-REQ-ARCH | **FLAG (F-1)** | a sealed path is edited and the permission is not stated |
| TR-SHIP-WITH | **PASS, exemplary** | 76 test files of 183 |
| TR-BASELINE-PROVENANCE | **PASS, exemplary** | see F-3 |
| **AR-ORG-CONTRACT-PURITY** | **FLAG (F-5)** | **new rule** — `ITcpRecovery` holds 5 no-op defaults |
| AR-ORG-DOMAINS, AR-ORG-VIS-SPLIT | PASS | 25 known couplings, none new |
| **AR-EXT-MINIMAL-SURFACE**, **AR-EXT-VIRTUAL-IS-A-PROMISE** | questions, not findings | **new rules** — see the notes |
| RR-WHATSNEW, RR-BREAK-MIGRATE, RR-DEPRECATE-FIRST | **FLAG (F-2)** | no entry |
| mergeability | **FLAG (F-6)** | 3 conflicting paths against current master |

## Findings

Numbering is carried from the first audit, so a citation of `F-1` still means the same thing.

### F-1 — The branch edits the sealed `common/packet/`, and does not say so

**Rules:** [PR-REQ-ARCH](../../../rule/pull-request.md#pr-req-arch),
[SR-FOLDER-RECURSIVE](../../../rule/sealing.md#sr-folder-recursive). **Blocking. Unchanged.**

Three files under the one sealed path, in two commits: `chunk/BitCountChunk.cc`,
`chunk/ByteCountChunk.cc` (`ed742203a8`), `recorder/PcapRecorder.cc` (`21d34de9e8`). No permission is
stated.

**And the seal is still not one this registry can defend.** Ten days on, `common/packet/` remains
sealed over `AV-ORG-01` and `AV-ORG-02`, both still `Open (decide)` — which
[SR-AUDIT-FIRST](../../../rule/sealing.md#sr-audit-first) forbids. Blocking a three-line chunk repair
behind a seal our own ledger calls invalid is still the worst of both. **Decide the seal.**

### F-2 — No release note, for a change that breaks users four ways

**Rules:** [RR-WHATSNEW](../../../rule/release.md#rr-whatsnew),
[RR-BREAK-MIGRATE](../../../rule/release.md#rr-break-migrate),
[RR-DEPRECATE-FIRST](../../../rule/release.md#rr-deprecate-first). **Blocking. Unchanged.**

`WHATSNEW` untouched; `doc/src/` gains 15 lines, none of which mention a break. The `sndNxt` signal
and statistic are removed; `TcpBaseAlg` and `TcpTahoeRenoFamily` are renamed, which stops every
out-of-tree TCP algorithm compiling; 41 public functions are removed; `PppTrailer` is removed; the
modernized defaults move 53 fingerprints.

Two things are worth adding since the first audit. **A `using TcpBaseAlg = TcpAlgorithmBase;` alias
for one release** costs one line and satisfies
[RR-DEPRECATE-FIRST](../../../rule/release.md#rr-deprecate-first). And **this finding has since
appeared in four more pull requests** — [#1154](pr-1154.md), [#1172](pr-1172.md),
[#1173](pr-1173.md), [#1145](pr-1145.md) — from two authors, and once correctly did *not* appear
([#1175](pr-1175.md)). Five occurrences say the obligation is not visible where it is incurred. The
change summary computes the exact list a `T3` check would need.

### F-3 — Two baseline-only commits

**Rule:** [PR-SPLIT-BASELINE](../../../rule/pull-request.md#pr-split-baseline). **Finding. The
defence has aged.**

`9f40ee337e` and `33e8b0d073` hold fingerprint `.csv` files and nothing else, each following the
source commit that moves them. The rule inverted on 2026-09-01: a regenerated baseline now travels
in the same commit as the source change, because a source commit without its baselines fails its own
fingerprint test and breaks
[PR-SERIES-BUILDS](../../../rule/pull-request.md#pr-series-builds).

The first audit recorded that the author had followed the rule as it then stood, and that remains
true of the day it was written. Ten days later the branch has not been updated, so the remedy is
simply owed: **squash each into the commit before it, keeping both messages.**

**Keep the messages.** `33e8b0d073` is still the best baseline provenance this audit has seen: the
branch tip was built in a separate workspace and the full suite run there, 53 rows move, every one
carries TCP traffic, each family named — and it records what it deliberately did *not* touch.

### F-4 — A rename commit that also edits 25 files

**Rule:** [PR-SPLIT-MOVE](../../../rule/pull-request.md#pr-split-move). **Finding. Unchanged.**

`ed2c8df034` renames three files at **53 %, 56 % and 64 %** similarity while editing twenty-five
others. Git only just detected them as renames; a few more edited lines and the `git blame` history
of the three central TCP flavour files would have ended there. A rename-only commit first, then the
rewrite.

### F-5 — `ITcpRecovery` holds five no-op default bodies

**Rule:** [AR-ORG-CONTRACT-PURITY](../../../rule/architecture.md#ar-org-contract-purity).
**Finding. New — the rule was written on 2026-09-07, six days after the first audit.**

The head carries **17** interface violations against master's 15. One of the two is
`IIeee80211Band`, which this branch does not touch — master repaired it after this branch's base, so
that difference is staleness, not fault. The other is this branch's own:

```cpp
class INET_API ITcpRecovery {
    virtual bool isDuplicateAck(const TcpHeader *, uint32_t) = 0;
    virtual void receivedAckForUnackedData(uint32_t) = 0;
    virtual void receivedDuplicateAck() = 0;
    virtual void segmentsAcked(uint32_t, uint32_t) {}      // ← five no-op defaults
    virtual void dataSent(uint32_t) {}
    virtual void segmentRetransmitted(uint32_t, uint32_t) {}
    virtual void onRexmitTimeout() {}
    virtual void reoTimeout() {}
};
```

Three classes implement it: `Rfc5681Recovery`, `Rfc6582Recovery`, `Rfc6675Recovery`. A recovery
algorithm that does not care about one of the five gets silence — which is convenient today and is
the exact shape that made `IIndicatorFigure::getNumSeries()` a silent break in
[#1125](pr-1125.md) F-2. Rename one of the five and every out-of-tree recovery keeps compiling with
its override no longer called.

This is a **new interface in a new architecture**, which is the cheapest possible moment to get it
right: there is no out-of-tree implementor yet to break.

**What would close it:** make the five pure and add `{}` to the three implementors where the
behavior really is nothing — three one-line overrides each, and each states its own answer. A
`TcpRecoveryBase` is the alternative if the defaults grow beyond nothing.

### F-6 — The branch no longer merges

**Rule:** [PR-REQ-CLEAN](../../../rule/pull-request.md#pr-req-clean). **Blocking, and purely
mechanical.**

Master advanced 99 commits since this branch's merge base. `git merge-tree` reports three
conflicting paths:

| Path | Why |
|---|---|
| `src/inet/linklayer/ppp/PppHeaderSerializer.cc` | the branch rewrites PPP for RFC 1661; master changed the same serializer |
| `src/inet/linklayer/ppp/PppHeaderSerializer.h` | the same |
| `tests/fingerprint/showcases.csv` | both sides re-recorded rows |

Nothing here is a judgment about the change. It is the cost of a 61-commit branch waiting, and it
grows. The `showcases.csv` conflict also means the 53 rows of F-3 must be re-measured against the
new master before they are trustworthy.

### F-7 — Seventeen commits have no body at all

**Rules:** [PR-MSG-BODY](../../../rule/pull-request.md#pr-msg-body),
[PR-MSG-WHY](../../../rule/pull-request.md#pr-msg-why). **Finding. New, and it is a fault in the two
earlier passes of this report as much as in the branch.**

**Both earlier versions of this report marked `PR-MSG-WHY` as a PASS**, with the words *"symptom,
cause and decision throughout"*. That was written after reading the messages that have a body. **17
of the 61 commits have none** — no body, no trailer, nothing under the subject line. The rule as it
then read said *"the body gives the reason"*, which is vacuously satisfied by a commit that has no
body, so nothing in the rule set asked the question and neither pass thought to.

Twelve are above the line the new gate draws at 50 changed lines:

| Lines | Commit | Subject |
|---:|---|---|
| 860 | `81713e23d9` | tcp: size segments against the space options actually leave |
| 769 | `6d3b0fdcdb` | tcp: judge loss by transmission time (RFC 8985) |
| 683 | `710b18208c` | tcp: TCP Fast Open (RFC 7413) |
| 579 | `2fec57756c` | tcp: connection plumbing the modern features share |
| 528 | `7327e5c5d2` | tcp: Accurate ECN |
| 476 | `eb549a12a5` | tcp: undo a reduction that turned out to be unnecessary |
| 238 | `4b46417a58` | tcp: probe the tail rather than waiting out the timeout |
| 189 | `1a9200e2a3` | tcp: separate the receive buffer from the advertised window |
| 110 | `3616a5a5ca` | tcp: keepalive probing (RFC 1122 4.2.3.6) |
| 76 | `a4317c0d3e` | tcp: the socket options the new behavior exposes |
| 58 | `838e6d254f` | tcp: learn how far the path reorders |
| 53 | `8bbf0d575b` | tcp: retransmission, persist and handshake timing |

Five more are below it and are the reviewer's to judge, not the gate's.

**The subjects are good, and that is the trap.** *"undo a reduction that turned out to be
unnecessary"* and *"probe the tail rather than waiting out the timeout"* read like a reason, so a
reader skimming `git log` feels informed. They are not reasons; they are restatements of what the
code now does. Under which condition was the reduction unnecessary, and what happens if the undo
fires wrongly? Which timeout, and why is a probe better than waiting? A 683-line implementation of
RFC 7413 names the RFC and says nothing about what of it was implemented, what was left out, or
which of its options INET now negotiates.

**This is out of character for the branch and for the project.** The other 44 commits include the
best baseline provenance this audit has seen (F-3). Across master's last 300 commits, only 4 % of
commits over 50 lines have no body, and six of the nine that do are the kinds where the change
explains itself. Across the other six audited pull requests the new gate reports **zero**. This
branch reports twelve.

**What would close it:** a body on each of the twelve. The author plainly has the reasons — the
neighbouring commits in the same series demonstrate it — so this is writing down what is already
known, not discovering it.

## Notes

Advisory. See [audit/README.md](../../README.md) §*How severe is a finding*.

- **Ten subjects run between 73 and 80 characters.** `ed742203a` (75), `21d34de9e` (73),
  `9de16ef37` (73), `05704c0e6` (75), `5b417088d` (80), `c0ec1b6a9` (79), `5d94c07e7` (76),
  `fea41a027` (75), `8522c2e47` (79), `4fe69b26f` (80).
  [PR-MSG-SUBJECT](../../../rule/pull-request.md#pr-msg-subject) aims for 72 and its gate fails above
  80; all ten are inside that band.

- **28 new public functions have no caller outside their own class**, and **95 new virtuals are
  overridden nowhere** — against 174 new public functions and 54 new extension points. The questions
  [AR-EXT-MINIMAL-SURFACE](../../../rule/architecture.md#ar-ext-minimal-surface) and
  [AR-EXT-VIRTUAL-IS-A-PROMISE](../../../rule/architecture.md#ar-ext-virtual-is-a-promise) put to
  every new member; both rules postdate the first audit by eight days.

  | Class | uncalled | unoverridden |
  |---|---:|---:|
  | `Rfc6675Recovery` | 11 | 21 |
  | `TcpConnection` | — | 24 |
  | `TcpAlgorithmBase` | 5 | 14 |
  | `TcpSackRexmitQueue` | — | 11 |
  | `Tcp` | 1 | 11 |
  | `TcpSocket` | 8 | — |
  | `TcpCubic` | — | 7 |

  Spot-checked by hand: `Rfc6675Recovery::mayUndo`, `nextSeg`, `packetDelayed`, `prrCwndReduction`
  and others are **public, virtual, and called only from `Rfc6675Recovery.cc` itself**. They are not
  overrides — `ITcpRecovery` declares none of them. They are internal steps of the RFC 6675
  algorithm that happen to be public and virtual.

  This is the largest instance either rule has seen, and it is a question for the author, not a
  verdict: *who is `prrCwndReduction` for, and what would an override of `nextSeg` be trying to do?*
  Where the answer is "nothing outside this class", `private` and non-virtual says so. Where it is a
  sentence, the sentence belongs in the comment. A class designed as a base for future flavours
  legitimately has virtuals; 95 of them made by reflex is a different thing.

- **Corrected 2026-09-11, a second time: every NED parameter default was wrong.** The extractor
  read a parameter's default by walking every literal under it, which swept in the literals of its
  nested properties. `msl`'s default read `s` — its `@unit`. `mss`'s read empty. And
  `tcpAlgorithmClass`'s read the `@examples` list, which **hid the single most user-visible line of
  this branch**: the default TCP flavour changing from `TcpReno` to `TcpCubic`. The extractor now
  takes the `value` attribute that `opp_nedtool` already provides. This branch's changed-parameter
  count rises from 1 to 9, and F-2 names them.

- **Corrected 2026-09-11: 31 of the reported removals were not removals.** The change summary
  compared members owner by owner, so a class rename showed as a long list of removals beside a long
  list of additions. `TcpBaseAlg` → `TcpAlgorithmBase` alone accounted for 22 of them. The summary
  now pairs a member across owners and reports three relations: **renamed with their class** when the
  old class owns nothing at the head, **moved to another class** when it still exists, and **split
  across classes** when one member became several. The removal count for this branch falls from 80 to
  49, and the public-function removals from 41 to 25. F-2 is corrected; it is still a finding, and
  still the largest break in the change.

- **`IIeee80211Band` is not this branch's fault.** The interface gate reports it on the head and not
  on master, because master repaired it after this branch's base. A gate delta between a branch and
  a moved master needs this check every time; the finding is F-5's one class, not two.

## Not verified

- **PR-SERIES-BUILDS**, over 61 commits
  ([TR-CI-EVERY-COMMIT](../../../rule/testing.md#tr-ci-every-commit)).
- **The 76 test files pass**, and the 53 re-recorded rows are right. Read, not run — and after F-6
  they must be re-measured against the new master in any case.
- **The 296 stale rows** the branch reported on master. Master has moved 99 commits since; the claim
  was credible at its tip and is not re-checkable from here. It still deserves its own sweep.
- **The RFC conformance** of ten new algorithms. A domain review, and the one this change most needs.
