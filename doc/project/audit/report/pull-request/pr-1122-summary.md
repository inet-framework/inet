# Change summary — INET pull request #1122, "queueing: let a packet classifier grow its output on demand"

> **Kind:** report · **Status:** generated, do not edit · **Seal:** none · **Owns:** — · **Stands on:** [rule/pull-request.md](../../../rule/pull-request.md), [design/protocol-anatomy.md](../../../design/protocol-anatomy.md)

Base `434658d729` (merge base with master) to head `2f94d752fb` — 10 commits, 14 files changed, 609 insertions(+), 39 deletions(-).

Generated by `opp_summarize_changes --pr 1122 --usage` (opp_repl). Re-generate it rather than editing it; the process is `opp_repl/plan/pending/change-summary.md`. The companion rule audit is [pr-1122.md](pr-1122.md): that one judges the *commits* against `PR-*`, this one states what the change does to the *interface*.

Extracted 2026-09-11 from the sources in 1.3 seconds; no build was needed. NED and message facts come from `opp_nedtool` and `opp_msgtool`, so they are those parsers' own view of the tree. C++ facts come from the fast header scan and are **indicative**: it joins multi-line declarations and excludes generated `*_m.h`, but it does not expand macros or resolve templates, so a declaration built by a macro is invisible to it.

## In one line

**11** added.

## Breaking — check these first

### Removed

_nothing removed_

### Changed

_nothing changed in place_

### Signature changed

_no signature changed_

### Renamed

_nothing renamed_

## Added

**1 NED parameter**

- `inet.queueing.classifier.DynamicClassifier.aggregatorSubmoduleName`

**1 C++ public function**

- `DynamicClassifier::canPushSomePacket(const cGate *)`

**9 C++ extension points**

- `BehaviorAggregateClassifier::canPushPacket(Packet *, const cGate *)`
- `DynamicClassifier::canCreateGateForPacket(Packet *)`
- `DynamicClassifier::createBranch()` — **overridden nowhere**
- `DynamicClassifier::createBranchModule(int, cGate *)` — **overridden nowhere**
- `DynamicClassifier::createGateForPacket(Packet *)`
- `DynamicClassifier::getClassIndex(Packet *)` — **overridden nowhere**
- `MultiFieldClassifier::canPushPacket(Packet *, const cGate *)`
- `PacketClassifierBase::canCreateGateForPacket(Packet *)` — 1 override
- `PacketClassifierBase::createGateForPacket(Packet *)` — 1 override

## Questions for the review

Counted by name over `src/` and `tests/`, so *uncalled* is exact and everything else is a hint. An override is not listed: its visibility is fixed by its base, and fulfilling a hook is not a new promise.

**3 new virtual(s) that nothing overrides** — what would an override do, and does a comment say so? ([AR-EXT-VIRTUAL-IS-A-PROMISE](../../../rule/architecture.md#ar-ext-virtual-is-a-promise))

- `DynamicClassifier::createBranch()`
- `DynamicClassifier::createBranchModule(int, cGate *)`
- `DynamicClassifier::getClassIndex(Packet *)`

## Not changed

C++ classes · project features · source folders · message enum values · message fields · message types · NED gates · NED properties · NED signals · NED statistics · NED types
