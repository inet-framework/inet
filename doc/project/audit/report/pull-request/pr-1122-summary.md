# Change summary — INET pull request #1122, "queueing: let a packet classifier grow its output on demand"

> **Kind:** report · **Status:** generated, do not edit · **Seal:** none · **Owns:** — · **Stands on:** [rule/pull-request.md](../../../rule/pull-request.md), [design/protocol-anatomy.md](../../../design/protocol-anatomy.md)

Base `434658d729` (merge base with master) to head `2f94d752fb` — 10 commits, 14 files changed, 609 insertions(+), 39 deletions(-).

Generated by the change summary of opp_repl. Re-generate it rather than editing it; the process is `opp_repl/plan/pending/change-summary.md`. The companion rule audit of the same pull request is [pr-1122.md](pr-1122.md): that one judges the *commits* against `PR-*`, this one states what the change does to the *interface*.

Extracted 2026-09-01 from the sources in 1.8 seconds; no build was needed. NED and message facts come from `opp_nedtool` and `opp_msgtool`, so they are those parsers' own view of the tree. C++ facts come from the fast header scan and are **indicative**: it joins multi-line declarations and excludes generated `*_m.h`, but it does not expand macros or resolve templates, so a declaration built by a macro is invisible to it.

## In one line

**11** added.

## Breaking — check these first

### Removed

_nothing removed_

### Changed

_nothing changed in place_

### Signature changed

_no signature changed_

### Renamed

_nothing renamed_

## Added

**1 NED parameter**

- `inet.queueing.classifier.DynamicClassifier.aggregatorSubmoduleName`

**1 C++ public function**

- `DynamicClassifier::canPushSomePacket(const cGate *)`

**9 C++ extension points**

- `BehaviorAggregateClassifier::canPushPacket(Packet *, const cGate *)`
- `DynamicClassifier::canCreateGateForPacket(Packet *)`
- `DynamicClassifier::createBranch()`
- `DynamicClassifier::createBranchModule(int, cGate *)`
- `DynamicClassifier::createGateForPacket(Packet *)`
- `DynamicClassifier::getClassIndex(Packet *)`
- `MultiFieldClassifier::canPushPacket(Packet *, const cGate *)`
- `PacketClassifierBase::canCreateGateForPacket(Packet *)`
- `PacketClassifierBase::createGateForPacket(Packet *)`

## Not changed

C++ classes · project features · source folders · message enum values · message fields · message types · NED gates · NED properties · NED signals · NED statistics · NED types
