All: import inet.common.INETDefs;
INETDefs.msg: ???

All: #define FOO 42

All:
const B PPP_HEADER_LENGTH = B(5);
const int RIP_HEADER_SIZE = 4;

EthernetFcsMode:
cplusplus {{
INET_API EthernetFcsMode parseEthernetFcsMode(const char *fcsModeString);
}}

All:
cplusplus

All:
  properties: ???
  fields: ???

All:
#include

All:
@fieldNameSuffix: ??? eliminate
@omitGetVerb: ??? eliminate

All:
@opaque: ??? really needed?

All:
@override: ??? really needed?

All:
typedef const IIeee80211Mode ConstIIeee80211Mode;

All:
@descriptor(false) vs @descriptor(readonly);

TCPConnection:
@descriptor(readonly); vs @readonly??? wtf?

All:
Constants{ -> Constants {

Chunk.msg:
_getChunk -> getChunk
_packet -> packet
@getter vs @getval

DYMOSequenceNumber: class DYMOSequenceNumber??? why class? primitive class??? wtf?

All:
@existingClass(true); vs @existingClass;

PacketDrop:
packet_ -> packet

IntServ.msg:
struct: why struct???
*_t is reserved for C++ standard

InterfaceEntry: eliminate @getter if possible

Ieee80211ControlInfo: @tostring??? wtf?

All:
@editable(false) vs @descriptor(readonly)??? and in general

EtherType.msg: replace with Protocol.h?
IPProtocolId.msg: replace with Protocol.h?

All:
@customize(); vs @customize; vs @customize(true); bah
