This example demonstrates the performance of the ip simulation.

N transmitting nodes are each sending a burst of k packets with
UPD. For every transmitting node there is a receiving node that
doesn't send packets itself. All nodes are connected to a single
router.
