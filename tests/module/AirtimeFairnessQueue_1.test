%description:

Behavioral test for AirtimeFairnessQueue (802.11 downlink airtime fairness).

An access point saturates three wireless clients with downlink UDP traffic on equally
good links, transmitting to sta[0] at 6 Mbps and to sta[1] and sta[2] at 54 Mbps (per-
receiver data-frame rate). The AP's transmit queue is an AirtimeFairnessQueue.

With airtime-fair scheduling the test asserts two properties:
  - every client is served (allServed): a regression guard against the overflow-lockout
    bug, where a slow client's slowly-draining backlog filled the shared capacity and
    starved the other clients to zero throughput;
  - the fast clients recover (fastRecovered): each 54 Mbps client receives meaningfully
    more than the 6 Mbps client, i.e. throughput follows the airtime share, not an equal
    per-frame split.

%file: test.ned

import inet.networklayer.configurator.ipv4.Ipv4NetworkConfigurator;
import inet.node.ethernet.Eth100M;
import inet.node.inet.StandardHost;
import inet.node.inet.WirelessHost;
import inet.node.wireless.AccessPoint;
import inet.physicallayer.wireless.ieee80211.packetlevel.Ieee80211ScalarRadioMedium;

network AirtimeFairnessTest
{
    parameters:
        int numStations = default(3);
    submodules:
        configurator: Ipv4NetworkConfigurator;
        radioMedium: Ieee80211ScalarRadioMedium;
        server: StandardHost;
        accessPoint: AccessPoint;
        sta[numStations]: WirelessHost;
    connections:
        accessPoint.ethg++ <--> Eth100M <--> server.ethg++;
}

%inifile: omnetpp.ini

[General]
network = AirtimeFairnessTest
ned-path = .;../../../../src
sim-time-limit = 2.5s
warmup-period = 1s
**.vector-recording = false

*.radioMedium.sameTransmissionStartTimeCheck = "ignore"
*.*.ipv4.arp.typename = "GlobalArp"

# equally good links, all stations ~8-9 m from the AP
*.sta[*].mobility.typename = "StationaryMobility"
**.mobility.initFromDisplayString = false
*.sta[*].mobility.initialX = 9m
*.sta[*].mobility.initialY = 8m + parentIndex() * 2m
*.sta[*].mobility.initialZ = 0m
*.accessPoint.mobility.initialX = 17m
*.accessPoint.mobility.initialY = 12m
*.accessPoint.mobility.initialZ = 0m

# 802.11g; AP transmits to sta[0] at 6 Mbps and to the others at 54 Mbps
*.sta[*].wlan[*].opMode = "g(erp)"
*.accessPoint.wlan[*].opMode = "g(erp)"
*.sta[*].wlan[*].bitrate = 54Mbps
*.accessPoint.wlan[*].bitrate = 54Mbps
*.sta[*].wlan[*].radio.transmitter.power = 100mW
*.accessPoint.wlan[*].radio.transmitter.power = 100mW
*.accessPoint.wlan[*].mac.dcf.rateSelection.dataFrameBitratePerReceiver = {"sta[0].wlan[0]": 6Mbps}

# saturating downlink: server sources one flow per station, stations sink
*.server.numApps = 3
*.server.app[*].typename = "UdpBasicApp"
*.server.app[*].destAddresses = "sta[" + string(index) + "]"
*.server.app[*].destPort = 5000 + index
*.server.app[*].messageLength = 1000B
*.server.app[*].sendInterval = 1ms
*.sta[*].numApps = 1
*.sta[*].app[0].typename = "UdpSink"
*.sta[*].app[0].localPort = 5000 + parentIndex()

# the airtime-fair transmit queue under test (fairnessEnabled defaults to true)
*.accessPoint.wlan[*].mac.dcf.channelAccess.pendingQueue.typename = "AirtimeFairnessQueue"
*.accessPoint.wlan[*].mac.dcf.channelAccess.pendingQueue.packetCapacity = 50

%postrun-command: gawk '/sta\[[0-9]+\]\.app\[0\] packetReceived:sum\(packetBytes\)/ { match($2, /sta\[([0-9]+)\]/, m); b[m[1]] = $4 } END { printf "allServed=%s\n", (b[0]>0 && b[1]>0 && b[2]>0) ? "YES" : "NO"; printf "fastRecovered=%s\n", (b[1] > b[0]*1.2 && b[2] > b[0]*1.2) ? "YES" : "NO"; printf "values sta0=%d sta1=%d sta2=%d\n", b[0], b[1], b[2] }' results/*.sca > verdict.out

%contains: verdict.out
allServed=YES
fastRecovered=YES

%postrun-command: grep "undisposed object:" test.out > test_undisposed.out || true
%not-contains: test_undisposed.out
undisposed object:
