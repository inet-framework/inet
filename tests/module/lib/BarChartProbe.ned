package inet.test.moduletest.lib;

//
// Reports what the bar chart figure of a statistic visualizer ends up displaying,
// so that a test can assert on the visualization itself rather than on the traffic
// that happens to feed it.
//
simple BarChartProbe
{
    @class(BarChartProbe);
}
