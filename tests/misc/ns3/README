This folder contain NS3 vs. INET comparison tests.

Steps to run the tests:
 - checkout the topic/ns3-comparison branch from INET
 - clone the patched NS3 repository from git@github.com:/omnetpp/ns3
 - compiled NS3 using the official mechanism (./build.py --enable-examples)
 - create 'ns3' symlink in this folder to your NS3 folder
 - execute runtest *.test to run all tests

