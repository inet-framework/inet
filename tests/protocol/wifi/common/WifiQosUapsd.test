%# expected-result: FAIL
%description:
Observe a QoS data frame ending a service period (EOSP). The real EOSP field
exists; no U-APSD buffering/trigger procedure is established by the current model.
This is a presence probe, not a complete U-APSD exchange test.

%file: WifiQosUapsd.cc

#include "ProtocolTest.h"

namespace inet {
namespace protocoltest {

Define_ProtocolTest(wifi_qos_uapsd)
{
    return ProtocolTest("wifi_qos_uapsd")
        .once(on("ap.wlan[0].mac").signal("packetSentToLower")
                  .filterPacket("Ieee80211DataHeader.eosp == true")
                  .describe("AP delivers buffered frame in U-APSD service period").within(2.5));
}

} // namespace protocoltest
} // namespace inet

%inifile: test.ini

[General]
*.tester.testName = "wifi_qos_uapsd"
include ../../ini/_n.ini
**.wlan[*].radio.receiver.energyDetection = -105dBm
include ../../ini/_base.ini

%contains: stdout
PROTOCOLTEST wifi_qos_uapsd: PASS
